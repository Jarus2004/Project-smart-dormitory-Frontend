# 2026-07-27 — AI-assisted changes

สรุปการเปลี่ยนแปลงที่ทำโดย AI Agent:

- ปรับ `src/services/api.ts` ให้ resolve API base URL ผ่าน helper กลาง และเปลี่ยน fallback เป็น `http://localhost:3000/api`
- เพิ่ม helper `src/services/api-url.js` สำหรับรวม logic การเลือก `VITE_API_URL` กับค่า fallback
- เพิ่ม declaration file `src/services/api-url.d.ts` เพื่อให้ TypeScript build ผ่านกับ helper JS ตัวใหม่
- เพิ่ม unit test `tests/unit/api-url.test.mjs` เพื่อยืนยันว่า env override ได้ และ fallback ไปพอร์ต `3000` เมื่อไม่กำหนดค่า
- เพิ่ม `npm test` script สำหรับรัน unit test แบบ built-in ของ Node
- ตรวจยืนยันด้วย `npm test` และ `npm run build` ผ่านสำเร็จ
- ปรับ `src/componants/addminpage/pages/Monitor/MonitorPage.tsx` ให้รองรับ full-frame backend detection flow โดยส่งภาพ webcam ไปยัง backend และใช้งาน overlay จาก event `scan.bounding_box`
- ยืนยันว่า `scan`, `heartbeat`, `live_frame` event contract ของ backend ถูกเก็บบน frontend พร้อม UI status และ toast notification

หมายเหตุ:

- จุดประสงค์หลักคือปิดสาเหตุของ `Network Error` ที่เกิดจาก frontend ชี้ไป backend port ผิด
