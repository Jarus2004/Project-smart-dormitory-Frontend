## 2026-08-15 — Tenant Wizard UI Restructuring, API Success Wrapper Fixes, and Admin Mapping Updates

### 1. Wizard UI Restructuring (Step 1 to 4)
- **Step 1 (Personal Info & Emergency Contact)**: Combined inputs, removing manual `citizenId` and `address` text boxes.
- **Step 2 (Room Preference & PromptPay)**: Shifted room selection here, rendering dynamic PromptPay QR code generation immediately.
- **Step 3 (Dual File Upload & Verification)**: Modified `DocumentUpload.tsx` to handle dual uploads. Erased the hardcoded mock/fallback user details inside the `.catch` block to ensure contracts only populate using verified OCR scanner outcomes.
- **Step 4 (Contract Review)**: Relocated agreement form to Step 4, prioritizing the extracted OCR fields for contract generation.

### 2. API Response Validation Fix
- Configured check expressions in `DocumentUpload.tsx` and `TenantApplication.tsx` to read `response.data?.statusCode === 200` and `response.status === 200` / `201` instead of `.success`. This fixed the bug where successful HTTP 200 OK uploads were processed as failure events.

### 3. Admin Data Integration Sync
- Restructured `normalizeTenant` in `DashboardContext.tsx` to match the PostgreSQL schema keys:
  - `fullName` -> `name`
  - `phone` -> `contact`
  - `yearLevel` -> `year`
- Added fallback from `roomNumber` to `preferredRoomType` (displaying value like "Floor 1 / Floor 2" or "Unassigned") before a specific room ID has been provisioned.
- Standardized the database uppercase status (`PENDING` -> `Pending`), enabling correct rendering of warnings/badges on the Admin Student dashboard page.
